search System.Base;
search System.Car;

bundle Default {
	class Bar {
		value : Int;
		New() {
			value := 13;
		}

		method : public : native : GetValue(), Int {
			return value;
		}
	}

	class Foo {
		function : Main(), Nil {
			DoStuff();
		}

		function : native : DoStuff(), Nil {
#		function : DoStuff(), Nil {
			Test->DoStuff();
			"Hello World"->PrintLine();

			b : Bar := Bar->New();
			b->GetValue()->PrintLine();	
		}
	}
}
