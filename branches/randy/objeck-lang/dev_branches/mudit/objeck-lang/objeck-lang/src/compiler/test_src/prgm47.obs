use IO;

bundle Default {
	class Test {
		function : Main(args : System.String[]), Nil {
			Run();
		}

		# function : Run(), Nil {		
		function : native : Run(), Nil {		
			vv := Directory->List("../..");
			vv->GetSize()->PrintLine();
			for(i : Int := 0; i < vv->GetSize(); i := i + 1;) {
				v : System.String := vv[i];
				v->PrintLine();
			};
		}
	}
}
