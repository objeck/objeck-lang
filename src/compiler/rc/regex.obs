use RegEx;

bundle Default {
	class Test {
		function : Main(args : String[]) ~ Nil {
			if(args->Size() = 2) {
				regex := RegEx->New(args[0]);
				regex->Find(args[1])->PrintLine();
			};
		}
	}
}
