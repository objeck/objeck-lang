use Struct;

bundle Default {
	class Test {
		function : Main(args : System.String[]) ~ Nil {
			Run();
		}
		
		function : Run() ~ Nil {
			keys := ["1", "2", "3"];
			vals := ["a", "b", "c"];
			hash := StringHash->New();
			each(i : vals) {			
				hash->Insert(keys[i], vals[i]->As(Base));
			};
			
			each(i : vals) {
				v := hash->Find(keys[i])->As(String);
				v->PrintLine();
			};
		}
	}
}
