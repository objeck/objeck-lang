bundle Default {
  class CharCode {
    function : Main(args : String[]) ~ Nil {
      Run();
    }

    function : native : Run() ~ Nil {
      i := 'a'->As(Int);
		i->PrintLine();
		c := i->As(Char);
		c->PrintLine();
    }
  }
}
