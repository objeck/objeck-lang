bundle Default {
	class Array {
		function : Main(args : String[]) ~ Nil {
			sum := 0;
			prod := 1;
	
			arg := [1, 2, 3, 4, 5];
			each(i : arg) {
			  sum += arg[i];
			  prod *= arg[i];
			};

			sum->PrintLine();
			prod->PrintLine();
		}
	}
}
