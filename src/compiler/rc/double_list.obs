class Hello {
	function : Main(args : String[]) ~ Nil {
		list := Collection.IntList->New();
#~
		list->AddBack(13);
		list->AddBack(31);
		list->AddFront(1);
		list->AddBack(3);
		list->Find(13);
~#
		list->Insert(100);
		list->Insert(10);
		list->Find(10);
		list->Insert(50);

		list->Rewind();
		while(list->IsEnd() = false) {
			list->Get()->PrintLine();
			list->Next();
		};

		list->Forward();
		while(list->IsEnd() = false) {
			list->Get()->PrintLine();
			list->Previous();
		};
	}
}
