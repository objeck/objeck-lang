class Hello {
	function : Main(args : String[]) ~ Nil {
		list := Collection.IntList->New();
		list->AddBack(13);
		list->AddBack(31);
		list->AddFront(1);
		list->AddBack(3);
		list->Find(13);
		list->Insert(100);

		list->Rewind();
		while(list->IsEnd() = false) {
			list->Get()->PrintLine();
			list->Next();
		};
	}
}
