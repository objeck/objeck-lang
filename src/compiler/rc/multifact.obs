class Multifact {
	function : Main(args : String[]) ~ Nil {
		v := Int->New[12];
		for(g := 1; g < 10; g+=1;) {
			for (n := 1; n < 11; n+=1;) {
				v[n] := (g < n)? v[n-g]*n : n;
				IO.Console->Print(v[n])->Print(" ");
			};
			IO.Console->PrintLine();
		};
	}
}
