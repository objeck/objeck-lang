class Hello {
  function : Main(args : String[]) ~ Nil {
    i := Foo();
    f : FloatHolder := i->As(Compare);
		f->Get()->PrintLine();
#   t : FloatHolder := Bar();
  }

  function : Foo() ~ String {
    return "Hello";
  }

  function : Bar() ~ Compare {
		return Nil;
	}
}

