<#class name=Foo request=res respeonse=res/> 
<html>
	<head>
	</head> 
		<b>
			<#code>res->GetRequestMethod();</#code>
			<#code>
				each(i:s) { req->GetRequesUri(); };
				each(i:s) { req->GetRequesUri(); };
				each(i:s) { req->GetRequesUri(); };
				each(i:s) { req->GetRequesUri(); };
				each(i:s) { req->GetRequesUri(); };
			</#code>
		</b>

	<body>
		
	</body>
</html> 
