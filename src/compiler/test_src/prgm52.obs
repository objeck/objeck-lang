use IO;
use Time;

bundle Default {
	class Hello {
		New() {
		}

		function : Main(args : System.String[]) ~ Nil {
			"Hello World!" ->SubString(3, 3)->PrintLine();
#			"Hello World!"->SubString(0, 3)->PrintLine();

#			Time();
		}

		function : native : Time() ~ Nil {
#		function : Time() ~ Nil {
			t := Date->New();
			Console->Print(t->GetHours())->Print(":")->Print(t->GetMinutes())->Print(":")->PrintLine(t->GetSeconds());
		}
	}
}
