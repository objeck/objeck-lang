use System;

bundle Default {
	class Test {
		function : Main(args : System.String[]) ~ Nil {
			x := 10;
			x->PrintLine();
			g := x * 88;
			(g - 4)->PrintLine();
			y := String->New("Randy");
			y->Append(9);
			y->PrintLine();
		}
	}
}
