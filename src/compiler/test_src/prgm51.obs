bundle Default {
	class MyThread from Thread {
		New() {
			Parent();
		}

		method : public : Run(param : System.Base) ~ Nil {
			s : String := param->As(System.String);
			s->PrintLine();
			913->PrintLine();

			m := ThreadMutex->New("bob");
			critical(m) {
				a := 18;
				g := 12;
			};
		}
	}

	class Hello {
		New() {
		}

		function : Main(args : System.String[]) ~ Nil {
#~
			array := Int->New[3];
			array[0] := 13;
			j := array[0];
			j->PrintLine();
~#
			Run();
		}

		function : Run() ~ Nil {
			t := MyThread->New();	
			s := "Pug";	
			t->Execute(s->As(Base));

			"Fin."->PrintLine();
		}
	}
}
