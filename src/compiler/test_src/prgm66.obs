use Structure;

bundle Default {
	class Test {
		function : Main(args : String[]), Nil {
			Run();
		}

		function : native : Run(), Nil {		
			ints := IntVector->New();
			ints->AddBack(23);
			ints->AddBack(145);
			ints->AddBack(17);
			ints->AddBack(8);
			
			ints->Sort();

			each(i : ints) {
				IO.Console->GetInstance()->Print(i)->Print(": ")->PrintLine(ints->Get(i));
			};
		}
	}
}
