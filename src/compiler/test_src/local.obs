bundle Default {
	class Hello {
		function : Main(args : System.String[]) ~ Nil {
			Run(8);
		}

		function : Run(a : Int) ~ Nil {
			i := 3 * a;
			if(i < 10) {
				x := 5 - 9;
				y := x;
			}
			else {
				x := i - 9;
				y := x;
			};
			i := -1;
		}
	}
}
