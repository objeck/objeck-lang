use Gtk2;

bundle Default {
	class GtkHello {
		function : Main(args : String[]) ~ Nil {
			window := GtkWindow->New();
			window->SetTitle("Goodbye, World!");
			window->Show();
			Appliction->Main();
		}
	}
}
