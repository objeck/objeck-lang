use ODBC;
use Structure;

bundle Default {
	class SqlTest {
		function : Main(args : String[]) ~ Nil {
			Run();
		}

		function : native : Run() ~ Nil {
			conn := Connection->New("test", "root", "");	
			if(conn <> Nil) {
				pstmt := conn->CreateParameterStatement("UPDATE test.student SET credits=? WHERE id=?");
#				pstmt->SetString(1, "Andrews");
				pstmt->SetInt(1, 10);
				pstmt->SetInt(2, 1);
				pstmt->Update()->PrintLine();
				pstmt->Close();
#~
				conn->Update("UPDATE test.student SET last_name='dog' WHERE id=2")->PrintLine();
				result := conn->Select("select * from test.student");
				if(result <> Nil) {
					i := 0;	
					while(result->Next()) {
						result->GetInt(1)->PrintLine();
						result->GetString(3)->PrintLine();
						ts := result->GetTimestamp(5);
						ts->GetYear()->PrintLine();
						i += 1;
					};
					i->PrintLine();	
					result->Close();
				};
~#
				conn->Close();
			};
		}
	}
}
