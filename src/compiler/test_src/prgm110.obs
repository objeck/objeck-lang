use RegEx;

bundle Default {
	class Hello {
		function : Main(args : String[]) ~ Nil {
#			regex := RegEx->New("[^e-g]i+j*");
#			regex := RegEx->New("[^e-g]i+j*");
			regex := RegEx->New("(r|j)+");
#			regex := RegEx->New("[rj]");
			regex->Apply("rjrj")->PrintLine();
		}
	}
}
