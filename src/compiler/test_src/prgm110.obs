bundle Default {
	class Foo {
		New() {
		}

		Remote() {
		}

		method : public : Bar(a : Int) ~ Nil {
			a->PrintLine();
		}
	}

	class Test {
		function : Main(args : String[]) ~ Nil {
			Run();
		}

		function : Run() ~ Nil {
#			ff := Foo->New();
			ff := Foo->Remote();
			ff->Bar(13);
		}
	}
}
