use RegEx;

bundle Default {
	class Hello {
		function : Main(args : String[]) ~ Nil {
#			regex := RegEx->New("ab[cd]ef[ghijk]l[a-z]");
			regex := RegEx->New("[abcd]");
			regex->Apply("b")->PrintLine();
		}
	}
}
