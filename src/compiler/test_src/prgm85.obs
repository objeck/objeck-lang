bundle Default {
	class Test {
		function : Main(args : String[]) ~ Nil {
			b : Base := "hello";
#			b : String := "hello";
			b->As(String)->Find(1, 'l')->PrintLine();
#			x := b->Find(1, 'l');
#			b->PrintLine();


			c : String := "hello";
			y := c->Find(1, 'l');
			y->PrintLine();
			c->PrintLine();


#~

#			ss : String[] := String->New[1];
			ss : String[];
			
			ss[0] := "world";
			ss[0]->PrintLine();
~#
		}
	}
}
