bundle Default {
	class DllTest {
		function : Main(args : String[]) ~ Nil {
			dll_proxy := DllProxy->New("lib/test.so.1.0.1");
			dll_proxy->CallFunction("foo", Nil->As(Base[]));
			dll_proxy->Unload();
		}
	}
}
