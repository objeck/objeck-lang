use System.IO.File;

class Test {
	function : Main(args : String[]) ~ Nil {
		IsEmptyDirectory(args[0])->PrintLine();
	}
	
	function : IsEmptyDirectory(dir : String) ~ Bool {
#	dir->PrintLine();
		return Directory->List(dir)->Size() = 0;
#		foo := Directory->List(dir);
#		return foo->Size() = 0;
	}
}
