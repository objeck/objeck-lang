class Gee {
	function : Main(args : String[]) ~ Nil {
		t := "   "->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');
		
		t := "   aaaa"->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');

		t := "bbb    "->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');

		t := "  abc  "->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');

		t := ""->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');
		
		t := "aaaa"->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');
	}
}
