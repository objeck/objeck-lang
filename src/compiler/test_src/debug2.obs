bundle Default {
	class Hello {
		function : Main(args : String[]) ~ Nil {
			i := 0;
			while(i < 3) {
				i += 3;
				x := i * 1;
			};
	
			x : Float;
			x->PrintLine();
			if(x < 3) {	
				y := 9;	
				y->PrintLine();
			};
			x->PrintLine();
		}
	}
}
