class Gee {
	New() {}
	
	method : public : A() ~ String {
		return "a";
	}
}

class Fee {
#~
	enum Fie { A, B }

	function : Bar() ~ Fee->Fie {
		return Fee->Fie->B;
	}
~#

	function : Main(args : String[]) ~ Nil {
#~
		s := "abc";
		s->SubString(0,1)->Trim;
~#
		g := Gee->New();
		g->A()->Trim;

#		Bar()->PrintLine();
	}
}
