use System.IO.File;

class Test {
	function : Main(args : String[]) ~ Nil {
		buffer := "";
		c : Compare := FloatHolder->New(13.13);
#		f := c->As(FloatHolder)->Get();
#		buffer += f;
		buffer += c->As(FloatHolder)->Get();
		buffer->PrintLine();
	}
}
