use Struct;

bundle Default {
	class Car {
		New() {}
	}

	class Ford from Car {
		New() { Parent(); x := 9; }
	}

	class Bike {
		New() { Parent(); }
	}

	class Plane {
		New() { Parent(); }
	}

	class Hello {
		function : Foo(a : Int) ~ Nil {
		}

		function : Foo(a : Float) ~ Nil {
		}

		function : Foo(a : Int[]) ~ Nil {
		}

		function : Bar(b : Bike, i : Int) ~ Nil {
		}

#~
		function : Bar(c : Ford) ~ Nil {
		}

		function : Bar(p : Car) ~ Nil {
			"Car"->PrintLine();
		}
~#

		function : Bar(p : Ford, i : Int, c : Char) ~ Nil {
			"Ford"->PrintLine();
		}

		function : Bar(p : Base, i : Int, f : Float) ~ Nil {
			"Bar"->PrintLine();
		}

		function : Main(args : String[]) ~ Nil {
#~
			a := Int->New[2];
			Foo(13);
~#
			c := Ford->New();
			Bar(c, 10, 'b');
		}
	}
}
