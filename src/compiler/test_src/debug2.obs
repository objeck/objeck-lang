class Gee {
	function : Main(args : String[]) ~ Nil {
		t := "   "->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');
		
		t := "   1234"->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');

		t := "1234    "->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');

		t := "  1234  "->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');

		t := ""->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');
		
		t := "1234"->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');
		
		t := "a"->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');

		t := " a"->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');

		t := "a "->Trim();
		IO.Console->Print('|')->Print(t)->PrintLine('|');
	}
}
