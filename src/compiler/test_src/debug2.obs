class Test {
	function : Main(args : String[]) ~ Nil {
		aa := Base->New[1];
    aa[0] := FloatHolder->New(13.13);

#~		
		v := aa[0]->As(FloatHolder);
		v->Get()->PrintLine();
~#

		v := aa[0];
		v->As(FloatHolder)->Get()->PrintLine();
	}
}
