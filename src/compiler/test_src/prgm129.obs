enum Foo {
	FIE,
	FOO,
	FUM
}

class Car {
	enum Part {
		TIRE,
		DOOR,
		ROOF
	}

  function : Main(args : String[]) ~ Nil {
	x := Car->Part->ROOF;
	x->PrintLine();
  
	f : Foo := Foo->FIE;
	p : Car->Part := Car->Part->ROOF;
	p->PrintLine();
	# p := f;
		
  }
}
