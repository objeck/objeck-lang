bundle Default {
	class Hello {
		target : static : String;
		
		function : Init() ~ Nil {
			target := "METHINKS IT IS LIKE A WEASEL";
		}
		
		function : fitness() ~ Nil {
# target->Size()->PrintLine();
# target->PrintLine();
#			"Hello"->PrintLine();
# target->Size()->PrintLine();			
    		for(i := 0; i < target->Size(); i += 1;) {
i->PrintLine(); 
"World"->PrintLine();

      			if(target->Get(i) = 'A') {
      				retVal += 1;
      			};
      			
      		};
    	}
    	
    	function : Main(args : String[]) ~ Nil {
			Init();
			
			for(i := 0; i < 150; i += 1;) {
#				"Foo"->PrintLine();
				fitness();
			};
		}
	}
}
