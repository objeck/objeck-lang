class Test {	
	function : Main(args : String[]) ~ Nil {
		v0 := Compare->New[4];
		v0[0] := "Randy";
		v0[1] := IntHolder->New(101);
		v0[2] := FloatHolder->New(3.33);
		v0[3] := IntHolder->New(0);
		
		v1 := Compare->New[4];
		v1[0] := "Jason";
		v1[1] := IntHolder->New(101);
		v1[2] := FloatHolder->New(2.75);
		v1[3] := IntHolder->New(1);
		
		v2 := Compare->New[4];
		v2[0] := "Joe";
		v2[1] := IntHolder->New(320);
		v2[2] := FloatHolder->New(3.9);
		v2[3] := IntHolder->New(2);
		
		v3 := Compare->New[4];
		v3[0] := "Henderson";
		v3[1] := IntHolder->New(121);
		v3[2] := FloatHolder->New(1.11);
		v3[3] := IntHolder->New(3);
		
		v4 := Compare->New[4];
		v4[0] := "Andrew";
		v4[1] := IntHolder->New(101);
		v4[2] := FloatHolder->New(2.75);
		v4[3] := IntHolder->New(4);
		
		table := Table->New(["name","class","grade","pk"], 3);
		table->AddRow(Row->New(v0));
		table->AddRow(Row->New(v1));
		table->AddRow(Row->New(v2));
		table->AddRow(Row->New(v3));
		table->AddRow(Row->New(v4));
		
		## find class
		size := table->Size();
		r0 := table->Find(IntHolder->New(101), Match->EQUAL, "class");
		found := r0->Size();
		"total={$size}, found={$found}"->PrintLine();
		
		r1 := r0->Find(FloatHolder->New(2.75), Match->EQUAL, "grade");
		found := r1->Size();
		"total={$size}, found={$found}"->PrintLine();
		
		while(r1->More()) {
			row := r1->Get();
			name := row->Get("name")->As(String);
			"name={$name}"->PrintLine();
			r1->Next();
		};
	}
}