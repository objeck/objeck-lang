bundle Default {
	class Test {
		function : Main(args : String[]) ~ Nil {
			'a'->ToUpper()->PrintLine();
			'Z'->ToUpper()->PrintLine();
			'R'->ToUpper()->PrintLine();
			'q'->ToUpper()->PrintLine();

			'a'->ToLower()->PrintLine();
			'Z'->ToLower()->PrintLine();
			'R'->ToLower()->PrintLine();
			'q'->ToLower()->PrintLine();
		}
	}
}
