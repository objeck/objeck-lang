interface Foo {
	method : virtual : public : Go(a : Int) ~ Float;
	method : virtual : public : Go2() ~ Int;
}

class Test {

	function : Main(args : String[]) ~ Nil {
#~
s :=				String->New() {
					New() { Parent(); }

					method : public : Bar() ~ Nil {
						"barred"->PrintLine();
					}
				};
		s->Bar();
		s->Append("GGG");
		s->PrintLine();
		s->GetClass()->GetName()->PrintLine();

		e := 2;
		c := Foo->New(10) {
			@m : Int;
			@e : Int;
			# TODO: make an explicit call...
			New(a : Int) { Parent(a); @m := 31; }

			method : public : Go(a : Int) ~ Float {
				Go2(4);
				return @z + @e * 3.145;
			}

			method : public : Go2() ~ Int {
				return 13;
			}
		};

		c->Go2(13);
		c->GetClass()->GetName()->PrintLine();
~#

		d := Foo->New(10) {
#			New(a : Int) { }
			New(a : Int) { }
			method : public : Go(a : Int) ~ Float {
				return a * 67.95;
			}

			method : public : Go2() ~ Int {
				return 101;
			}
		};

		d->Go2()->PrintLine();
		d->Go(31)->PrintLine();
		d->GetClass()->GetName()->PrintLine();
	}
}
