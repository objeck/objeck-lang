use IO;

bundle Default {
	class Test {
		function : Foo(i : Float, j : Float) ~ Float {
			return i * j;
		}

		function : Main(args : String[]) ~ Nil {
			writer : FileWriter;
			writer->WriteByte(13);
			if(false) {
				i := 10;
			};

			if(i = 10) {
				i += 10;
			};
		}
	}
}
