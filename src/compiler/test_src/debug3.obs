# func/mthds sigs
# items: class vs. int
# internal/external refs
# lib support
#	- full resolution
#	- already named
	
class Test {
	enum DataType { A, B, C, D }

  function : Foo(p : DataType) ~ Nil {
	}

  function : Main(args : String[]) ~ Nil {
  }
}
