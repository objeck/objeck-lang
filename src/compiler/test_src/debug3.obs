# func/mthds sigs
# items: class vs. int
# internal/external refs
# lib support
#	- full resolution
#	- already named
# 
# DataType
# change "#" to ":" for write out and lib load
	
class Main {
	enum Type { A, B, C, D }

  function : Foo(p : Type) ~ Nil {
		p->PrintLine();
	}

  function : Bar() ~ Type {
#  function : Bar() ~ Type {
		return Main->Type->D;
	}

  function : Main(args : String[]) ~ Nil {
#		Main->Foo(Main->Type->C);
  }
}
