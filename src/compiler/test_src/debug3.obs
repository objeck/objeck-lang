# func/mthds sigs
# items: class vs. int
# internal/external refs
# lib support
#	- full resolution
#	- already named
# 
# DataType
	
class Main {
#~
	enum Type { A, B, C, D }

  function : Foo(p : Type) ~ Nil {
		p->PrintLine();
	}
~#
  function : Main(args : String[]) ~ Nil {
		Test->Foo(Test->Type);
  }
}
