use System.IO.File;

class FooBar {
	function : Main(args : String[]) ~ Nil {
		f := FileReader->New("abc");
		l := f->ReadString();
		while(l <> Nil) {
			# update
			l := f->ReadString();
		};

		leaving {
			if(f <> Nil & f->IsOpen()) {
				f->Close();
			};
		};
	}
}
