use Struct;
use RegEx2;

bundle Default {
	class Test {
		function : Main(args : String[]) ~ Nil {
			if(args->Size() = 2) {
				regex := RegExParser->New(args[0]);
				regex->Search(args[1]);
			};
		}
	}
}
