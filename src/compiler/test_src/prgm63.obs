bundle Default {
	class Test {
		function : Main(args : System.String[]), Nil {
			Run();
		}

#		function : Run(), Nil {
		function : native : Run(), Nil {		
			(13 and 7)->PrintLine();
			a : Int := 13;
			(a and 7)->PrintLine();
			(7 and a)->PrintLine();

			s := String->New();
			s->GetUniqueID()->PrintLine();

			a := 13;
			b := 7;
			(a xor b)->PrintLine();
			(13 xor b)->PrintLine();
			(7 xor a)->PrintLine();
			(7 xor 13)->PrintLine();

			i := "hollines"->GetUniqueID();
			i->PrintLine();
			hash := 2 xor i % 4027;
			hash->PrintLine();

			i := "randy"->GetUniqueID();
			i->PrintLine();
			hash := 2 xor i % 4027;
			hash->PrintLine();

			i := "pug"->GetUniqueID();
			i->PrintLine();
			hash := 2 xor i % 4027;
			hash->PrintLine();
		}
	}
}
