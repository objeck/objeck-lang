use System.IO.File;

class FooBar {
	function : Main(args : String[]) ~ Nil {
		a := false;
		b := 0;
#		if(false := a + b) {
		if(<>a) {
			"false"->PrintLine();
		};
	
		f : FileReader;

		if(args->Size() = 1) {
			f := FileReader->New(args[0]);
			l := f->ReadString();
			while(f->IsEOF() = false) {
				l->PrintLine();
				l := f->ReadString();
				return;
			};
		};

#~
FooBar
FooBar
FooBar
FooBar
FooBar
FooBar
~#
		leaving {
			if(f <> Nil & f->IsOpen()) {
				f->Close();
				"Closed."->PrintLine();
			};
		};

		"Done."->PrintLine();
	}
}
