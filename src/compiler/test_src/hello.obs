bundle Default {
	class Test {
		function : Main(a : String[]) ~ Nil {
			Run()->PrintLine();
		}

		function : Run() ~ Int {
#		function : native : Run() ~ Int {
			x : Int;
			for(i := 0; i < 10000;  i+= 1;) {
				for(j := 0; j < 10000; j += 1;) {
					x += i * j;
				};
			};

			return x;
		}
	}
}
