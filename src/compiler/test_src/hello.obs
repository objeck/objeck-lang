bundle Default {
  class Foo {
    New() {
    }
  }
  
	class Hello {
		function : Main(args : String[]) ~ Nil {
#			f := Foo->New();
#			c := Class->New("Foo");
			f := Foo->GetClass()->NewInstance();	
		}
	}
}
