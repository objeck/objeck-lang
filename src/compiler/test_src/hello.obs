bundle Default {
	class Test {
		@x : Float;

		function : Main(a : String[]) ~ Nil {
			test := Test->New();
			test->SetX(3.145);
			test->GetX()->PrintLine();
		}

		New() {}

		method : SetX(x : Float) ~ Nil {
			@x := x;
		}	

		method : GetX() ~ Float {
			return @x;
		}
	}
}
