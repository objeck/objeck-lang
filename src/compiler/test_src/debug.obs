class Foo {
	New() {}
}

class Bar from Foo {
	New() { Parent(); }
}

class Test {
	function : Get() ~ Foo {
		return Bar->New();
	}

	function : Main(args : String[]) ~ Nil {
		fb := Get()->As(Foo);
		fb->GetInstanceID()->PrintLine();	
#		str := String->New()->As(Base);
#		str->GetInstanceID()->PrintLine();
	}
}
