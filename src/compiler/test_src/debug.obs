use Collection;

class Test {
	function : Main(args : String[]) ~ Nil {
		values := CompareVector->New();
		Foo(values);
	}

	function : Foo(values : CompareVector) ~ Nil {
		v := values->ToArray();
		s := values->Size();
	
		v->Size()->PrintLine();
		s->PrintLine();
	}
}
