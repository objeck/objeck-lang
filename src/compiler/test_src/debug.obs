class Foo {
	@a : Int;
	@b : Int;

	New() {
		@a := 13;
		@b := 7;
	}
	
	method : public : Foo() ~ Nil {
#		@a->PrintLine();
#		@b->PrintLine();

#		a := @a;
#		b := @b;

		"blah blah {$@a} what not."->PrintLine();
		"blah blah {$@b} what not."->PrintLine();
	}
}

class Bar {
	function : Main(a : String[]) ~ Nil {
		f := Foo->New();
		f->Foo();
	}
}
