class RowEchelon {
	function : Main(args : String[]) ~ Nil {
		in := "Rosetta code"->ToByteArray();
#		in := Byte->New[3];
		out := Encryption.Hash->SHA256(in);

		a  := out[1]->As(Int);
		a->PrintLine();

		b := out[16]->As(Int);
		b->PrintLine();
	}
}
