class Test {
	function : Main(args : String[]) ~ Nil {
		v := Collection.IntVector->New();
		v->AddBack(13);
		v->AddBack(7);
		v->AddBack(10);
		v->Remove(1)->PrintLine();
		v->Remove(1)->PrintLine();
		v->Remove(0)->PrintLine();

		"-----"->PrintLine();

		each(i : v) {
			v->Get(i)->PrintLine();
		};
	}
}
