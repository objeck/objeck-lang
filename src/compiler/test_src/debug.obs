class Token {
	@type : Token->Type;

	function : public : Main(a : String[]) ~ Nil {
		t := Token->New();
		t->GetType()->PrintLine();
	}

	New() {
		@type := Token->Type->ARRAY;	
	}

	method : public : GetType() ~ Token->Type {
		return @type;
	}

	enum Type := -100 {
 		NUMBER,
		STRING,
		ARRAY,
		STRUCT
	} 
}
