bundle Default {
	enum Words {
		Fie,
		Foo,
		Fum
	}
		
	class Test {
		function : Main(args : String[]) ~ Nil {
			b := "Hello World!"->ToCharArray();
			String->New(b, -1, 10)->PrintLine();
		}
	}
}
