use Struct;

bundle Default {
	class Foo {
		New() {}
	
		method : public : Size() ~ Int {
			return 13;
		}
	}

	class Test {
		function : Main(args : String[]) ~ Nil {
			v := Vector->New();
			v->AddBack(Foo->New());
			v->AddBack(Foo->New());
			v->AddBack(Foo->New());

			each(i : v) {
#				a := v->Get(i)->As(Foo)->Size();
				v->Get(i)->As(Foo)->Size();
			};
		}
	}
}
