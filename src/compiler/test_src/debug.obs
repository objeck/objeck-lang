use Struct;

bundle Default {
	class Foo {
		New() {}
	}

	class Test {
		function : Main(args : String[]) ~ Nil {
			v := Vector->New();
			v->AddBack("one");	
			v->AddBack(Foo->New());
			v->AddBack("three");	

			each(i : v) {
				s := v->Get(i)->As(String)->Size();
				s->PrintLine();
			};
		}
	}
}
