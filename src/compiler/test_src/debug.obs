class Foo {
	@id : Int;
	New() { @id := 13; }
}

class Bar from Foo {
	@id : Int;

	New() { Parent(); @id := 7; }

	method : public : GetId() ~ Int {
		return @self->@id;
	}
}

class Test {
	function : Main(args : String[]) ~ Nil {
		b := Bar->New();
		b->GetId()->PrintLine();
	}
}

