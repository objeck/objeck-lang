bundle Default {
	class Test {
		function : Main(args : String[]) ~ Nil {
			Run();
		}

		function : Run() ~ Nil {
			v := Struct.Vector->New();
			v->AddBack("foo");
			v->AddBack("bar");
			IO.Console->Print("string: token count=")->PrintLine(v->Size());
		}
	}
}
