use System.IO.File;

class Test {
	function : Main(args : String[]) ~ Nil {
		user_dir := Runtime->GetProperty("user_dir");
		user_dir->PrintLine();

		tmp_dir := Runtime->GetProperty("tmp_dir");
		tmp_dir->PrintLine();
	}
}
