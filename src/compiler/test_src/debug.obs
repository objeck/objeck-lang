bundle Default {
	class Fum {
		function : Main(args : String[]) ~ Nil {
			a := ""->Trim();
			IO.Console->Print("'")->Print(a)->PrintLine("'");

			a := "  "->Trim();
			IO.Console->Print("'")->Print(a)->PrintLine("'");

			a := "abc	"->Trim();
			a := "	abc"->Trim();
			IO.Console->Print("'")->Print(a)->PrintLine("'");

			a := "abc	"->Trim();
			IO.Console->Print("'")->Print(a)->PrintLine("'");

			a := "abc"->Trim();
			IO.Console->Print("'")->Print(a)->PrintLine("'");
		}
	}
}
