class Foo {
	New() {
	}
}
	
class Hello {
	function : Main(args : String[]) ~ Nil {
		values := Collection.List->New();
#		f := Foo->New();
		f := String->New();
#		f : Compare := Foo->New()->As(Compare);
		values->AddBack(f->As(Compare));
	}
}
