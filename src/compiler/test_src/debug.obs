class Foo {
	New() {
	}
}
	
class Hello {
	function : Main(args : String[]) ~ Nil {
		values := Collection.List->New();
		values->AddBack(Foo->New()->As(Compare));
		values->Find(Foo->New()->As(Compare));
	}
}
