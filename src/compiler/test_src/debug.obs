bundle Default {
	class Hello {
		function : Main(args : String[]) ~ Nil {
			x := true ? "foo" : "bar";
#~
			if(false) {
				x := "foo";
			}
			else {
				x := "bar";
			};
~#

			x->PrintLine();
		}
	}
}
