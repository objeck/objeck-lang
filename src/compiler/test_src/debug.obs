enum Foo := -100 { THUG, LIFE, BABY }
class Bar {
	function : Main(a : String[]) ~ Nil {
		out := "";
		in := "Hello World!";
		each(i : in) {
			out->Append(in->Get(i)->ToUpper());
#			out->Append(in->Get(i));
		};
		out->PrintLine();
	}
}
