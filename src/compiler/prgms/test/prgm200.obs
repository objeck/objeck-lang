bundle Default {
	class Hello {
		function : Main(args : String[]) ~ Nil {
			Run(13);
		}

		function : native : Run(b : Int) ~ Nil {
			a := 3 + b;
			while(a > 0) { 
				z := 13;
				c := a * 9 + z;
				a -= 1;
			};
			a := 100;
		}
	}
}
