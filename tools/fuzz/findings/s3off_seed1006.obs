#~
Reduced fuzzer finding (tools/fuzz/reduce.py) from seed 1006.
signature: diverge: s0/off,s3/default,s3/jit1,s0/jit1 | s3/off first=F#=# vs F#=#
partition: s0/off,s3/default,s3/jit1,s0/jit1 | s3/off
~#
#~
Generated by tools/fuzz/gen.py (seed=replay, features=F1,F5).
Deterministic; prints one digest line per top-level function.
~#

class FuzzProgram {
  function : G0(x : Int) ~ Int {
    return x;
  }

  function : Apply(f : (Int) ~ Int, x : Int) ~ Int {
    return (f(x) + f(x + 1)) % 1048573;
  }

  function : F0(p0 : Int) ~ Int {
    v0 := p0;
    i0 := 0; w0 := 0;
    i1 := 0; w1 := 0;
    i2 := 0; w2 := 0;
    a0 := Int->New[16];
    for(i0 := 0; i0 < a0->Size(); i0 += 1;) { a0[i0] := (i0 * 1 + p0) % 1009; };
    a1 := Int->New[16];
    for(i0 := 0; i0 < a1->Size(); i0 += 1;) { a1[i0] := (i0 * 1 + p0) % 1009; };
    fr0 : (Int) ~ Int := G0(Int) ~ Int;
    k0 : FuncRef<IntRef> := \() => IntRef->New((0 - 20));
    kr0 := k0();
    v0 := (v0 + kr0->Get()) % 1048573;
    v0 := p0;
    return p0;
  }

  function : Main(args : String[]) ~ Nil {
    d := 0;
    r := 0;
    d := 0;
    for(r := 0; r < 12; r += 1;) {
      d := (d * 31 + F0(r)) % 1000000007;
    };
    "F0="->Print(); d->PrintLine();
  }
}
