bundle Default {
  class Foo {
    New() {
    }
  }

  class UID {
    function : Main(args : System.String[]), Nil {
#~
      Float->Pi()->PrintLine();
~#

      s := String->New();
      i := s->GetClassID();
      i->PrintLine();

      s := String->New();
      s->GetClassID()->PrintLine();
    }
  }
}
