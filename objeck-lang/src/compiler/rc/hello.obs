bundle Default {
	class Hello {
		function : Main(args : String[]) ~ Nil {
			2.3->PrintLine();
			Run(10, 3.55);
		}

#		function : native : Run(a : Int) ~ Float {
		function : native : Run(a : Int, b : Float) ~ Nil {
			a += 3;
			c := 3 - 5.6;
			a->PrintLine();
			c->PrintLine();
		}
	}
}
