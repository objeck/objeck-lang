bundle Default {
	class Hello {
		function : Main(args : System.String[]), Nil {
			Run();
		}
	
		function : native : Run(), Nil {
			i := 0;
			do {
				i->PrintLine();
				i := i + 1;
			}	
			while(i < 0);
			"Bye"->PrintLine();

			s := "Hello World!";
			s->StartsWith("Hello")->PrintLine();
			s->EndsWith("World!")->PrintLine();

			3->Max(5)->PrintLine();
			"Hello World!"->StartsWith("Hello ")->PrintLine();
			x : Int[,,] := [[[2,3,5][6,7,8][9,10,11]]]->As(Byte[,,]);
			y := [2,3,5,6,7,8];
			z : String[,] := [["Randy"]["Hollines"]];
			
			r : Int[] := [2,3,5,6,7,8]->As(Byte[]);
			s : String := "Hello World!";
		}
	}
}
