bundle Default {
	class MyThread from Thread {
	}

	class Hello {
		New() {
		}

		function : Main(args : System.String[]), Nil {
			Run(args);
		}

		function : Run(args : String[]), Nil {
			
#~
			(2 * 3)->PrintLine();
			critical {	
				a : Int := 8;
				(a-3)->PrintLine();
			};
~#
		}
	}
}
