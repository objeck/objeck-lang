bundle Default {
	class Foo {
		value : Int;

		New(v : Int) {
			value := v;
		}

		method : public : GetValue(), Int {
			return value;
		}	
	}

	class Test {
		function : Main(args : System.String[]), Nil {
			Run();
		}

#		function : Run(), Nil {
		function : native : Run(), Nil {		
			a : Int := 70;
			(a / 2)->PrintLine();
			(4 * a)->PrintLine();

			g : Float := 3;
			c : Float := 3.1415;
			(c-5.5)->PrintLine();
			d : Float := 11.12;

			z := Int->New[5,6];	
			z[2,3] := 27;

			h : Float := 11;
			(c-d)->PrintLine();
			(d-c)->PrintLine();		

			(g-h)->PrintLine();
			(h-g)->PrintLine();		

			f := Foo->New(24);
			v := f->GetValue();
		}
	}
}
