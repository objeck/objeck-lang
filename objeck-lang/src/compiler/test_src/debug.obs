bundle Default {
	class Hello {
		function : Main(args : String[]) ~ Nil {
			Run();
		}
	
		function : native : Run() ~ Nil {
			i := 4;
			s := String->New[i];
			s[0] := String->New();
			s[1] := String->New();
		}
	}
}
