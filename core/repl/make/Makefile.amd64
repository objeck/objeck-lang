ARGS=-O3 -Wall -std=c++17 -D_NO_HALT -D_X64 -D_OBJECK_NATIVE_LIB_PATH -Wno-unused-function
SRC=editor.o repl.o
OBJ_LIBS=../module/module.a
MODULE_PATH=../module
EXE=obi

$(EXE): $(SRC) $(OBJ_LIBS)
	$(MAKE) -m64 -o $(EXE) $(SRC) $(OBJ_LIBS) ../compiler/compiler.a ../compiler/logger.a ../vm/vm.a ../vm/memory.a ../vm/arch/jit/amd64/jit_common.a ../vm/arch/jit/amd64/jit_amd_lp64.o -ldl -lcrypto -lreadline -lssl -lz

%.o: %.cpp
	$(MAKE) -m64 $(ARGS) -c $< 

../module/module.a:
	cd $(MODULE_PATH); $(MAKE) -f make/Makefile.amd64

clean:
	cd $(MODULE_PATH); $(MAKE) -f make/Makefile.amd64 clean
	rm -f $(EXE) *.a *.o *~
