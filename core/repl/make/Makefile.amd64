CC=g++
ARGS=-O3 -Wall -std=c++17 -D_X64 -D_OBJECK_NATIVE_LIB_PATH -Wno-unused-function
SRC=editor.o repl.o
OBJ_LIBS=../module/objeck.a
MODULE_PATH=../module
EXE=obi

$(EXE): $(SRC) $(OBJ_LIBS)
	$(CC) -m64 -o $(EXE) $(SRC) $(OBJ_LIBS) ../compiler/compiler.a ../compiler/logger.a ../vm/memory.a ../vm/jit_amd_lp64.a ../vm/vm.a ../vm/arch/jit/amd64/jit_common.a -ldl -lcrypto -lreadline -lssl -lz

%.o: %.cpp
	$(CC) -m64 $(ARGS) -c $< 

../module/objeck.a:
	cd $(MODULE_PATH); make -f make/Makefile.amd64

clean:
	cd $(MODULE_PATH); make -f make/Makefile.amd64 clean
	rm -f $(EXE) *.a *.o *~
