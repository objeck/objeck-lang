@echo off
REM Windows bootstrap script
REM For Windows builds, the bootstrap process is handled by deploy_windows.cmd
REM which rebuilds everything including libraries, so this script is a no-op

echo Windows bootstrap: No separate bootstrap needed (handled by deploy_windows.cmd)
exit /b 0
