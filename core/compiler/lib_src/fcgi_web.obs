use System.IO;
use System.API;
use Collection.Generic;
use Web.HTTP;

#~
TODO (fcgi_web.obl) 
~#
bundle Web.FastCgi {
	class Session {
		function : GetUser(req : Request, res : Response) ~ User {
			return Nil;
		}
	}

	class User {
		@id : String;

		New(id : String) {
			@id := id;
		}
	}
}