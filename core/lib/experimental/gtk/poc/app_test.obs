use Gtk3;

# Music to code PoC to: https://www.youtube.com/watch?v=bRXnRwm-njM
class Gtk3Test {
	function : Activate(object : GObject, data : Base) ~ Nil {
		hi_str := data->As(String);
		hi_str->PrintLine();
		
		app := object->As(Application);
		win := app->WindowNew();
		win->SetTitle("Window");
		win->SetDefaultSize(200, 200);
		win->ShowAll();
	}

	function : Main(args : String[]) ~ Nil {
	
		app := Application->New("org.gtk.example", GApplicationFlags->G_APPLICATION_FLAGS_NONE);
		app->SignalConnect("activate", Activate(GObject, Base) ~ Nil, "Hi Ya!");
		app->Run(args);
#~	
		float_array : Float[] := [1.0, 2.1, 3.3];
		Tester->GetFloatArrayElement(float_array, 1)->PrintLine();

		char_array : Char[] := ['a', 'c', 'd', 'e'];
		Tester->GetCharArrayElement(char_array, 2)->PrintLine();

		byte_array := Byte->New[4];
		byte_array[0] := 0x1;
		byte_array[1] := 0xa;
		byte_array[2] := 0x9;
		byte_array[3] := 0xe;
		Tester->GetByteArrayElement(byte_array, 2)->PrintLine();
~#		
	}
}

