class Foo {
	New() {}
}

class Test {
	function : Main(args : String[]) ~ Nil {
#~
		h : BoolHolder := BoolHolder->New(true);
		h->Get()->PrintLine();

		x : Bool;
		a : BoolHolder := false;		
		x := a;
		a := x;
		x->PrintLine();
		a->Get()->PrintLine();
~#

		y : Int;
		b : IntHolder := 13;
		y := b;
		b := y;
		y->PrintLine();
		b->Get()->PrintLine();
	}
}
