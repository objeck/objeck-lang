class Test {
	function : Main(args : String[]) ~ Nil {
#		a : String;
#		b : String;
#		c := a + b;
		s : String := Fie->C; # FIXME

#		a : IntHolder := Fie->B;
#		a->Get()->PrintLine();
#		b := s + Fie->C;
#		b->PrintLine();
	}
}

enum Fie {
	A, B, C
}