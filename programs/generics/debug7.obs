 #~
	testing:
		* generics with boxing
~#

class Test {
	function : Main(args : String[]) ~ Nil {
		v : Bar<IntHolder> := Bar->New(13)<IntHolder>;
		v->Apply(Add(IntHolder) ~ IntHolder);
		v->Get()->Get()->PrintLine();
	}

	function : Add(s : IntHolder) ~ IntHolder {
#		s := 7 + s;
s->Get()->PrintLine();
#		s := s + 7;
		s += s + 5; # THIS SHOULD WORK
s->Get()->PrintLine();		
		return s;
	}
}