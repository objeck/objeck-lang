#~
# compile: obc -src fs_query_12.obs -lib query,gen_collect,regex,misc -dest fs_query_12.obe
# run: obr fs_query_12.obe
~#

use Query.Structured;

class Example {
	function : Main(args : String[]) ~ Nil {
		table := Table->FromFilesystem(".");
		if(table <> Nil) {
			results := table->Query("from files");
			if(results <> Nil) {
				results->ToString()->PrintLine();
			}
			else {
				table->GetError()->PrintLine();
			};
		};
	}
}
