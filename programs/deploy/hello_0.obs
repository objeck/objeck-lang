#~
# compile: obc -src hello_0.obs
# run: obr hello_0.obe
~#

class Hello {
  function : Main(args : String[]) ~ Nil {
 a := "!";
    "Hello World {$a}"->PrintLine();
    "Καλημέρα κόσμε" → PrintLine();
    "こんにちは 世界" → PrintLine();
  }
}
