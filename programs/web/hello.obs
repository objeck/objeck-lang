#~
# compile: obc -src hello_0.obs
# run: obr hello_0.obe
~#

class Hello {
  function : Action(req : Web.Server.Request, res : Web.Server.Response) ~ Nil {
    res->SetContentType("text/html");
    res->AppendBody("<html>Objeck is Back on the Web!</html>");
  }
}
