use Collection;

class Test {
	function : Main(args : String[]) ~ Nil {
		vector := CompareVector->New()<String>;
		vector->AddBack("Hello");
		vector->AddBack("World!");
		vector->AddBack("Again");

		vector->Sort();
		each(item := vector) {
			item->PrintLine();
		};

		queue := Queue->New()<IntRef>;
		queue->Add(38);
		queue->Add(438);
		queue->Add(8);
		queue->Add(-67);

		queue->Head()->PrintLine();
	}
}