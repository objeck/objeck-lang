use API.Ollama;

class Test {
    function : Main(args : String[]) ~ Nil {
        if(args->Size() = 1) {
            prompt := args[0];
            Completion->Generate("llama3", prompt)->PrintLine();
        };
    }
}