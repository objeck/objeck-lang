class Foo {
	function : Bar() ~ Nil {
		y := 7; 
		z := 4;

		f := \(Int) ~ Int : (x) => x * y + z;
		Func(f);

		i := 100;
		while(--i > 0) {
			x := "foo Bar";
			x->Size()->PrintLine();
		};
	}	

	function : Func(f : (Int) ~ Int) ~ Nil {		
		f(13)->PrintLine();
		Func2(f);
	}

	function : Func2(f : (Int) ~ Int) ~ Nil {		
		f(13)->PrintLine();
	}	

	function : Calc1(x : Int) ~ Int {
		return x + x;
	}
}

