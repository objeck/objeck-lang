
use Collection.Generic;
use Query.RegEx;

class Test {
	function : Main(args : String[]) ~ Nil {
		regex := RegEx->New("[aeiou]");
		if(regex->IsOk()) {
			regex->Find("This is a test sentence.")->Size()->PrintLine();
			regex->Match("aa")->PrintLine();
		};
	}
}

