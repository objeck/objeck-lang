class Repl {
    function : Main(args : String[]) ~ Nil {
        x := 2.3;
#~
        for(i := 5; i < x; i += 3;) {
            i->PrintLine();
        }        
~#
              
        each(i : FloatRange->New(10., 20., .5)) {
            i->PrintLine();
        };
    }
}