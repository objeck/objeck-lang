class Repl {
    function : Main(args : String[]) ~ Nil {
        x := 2.3;
#~
        for(i := 5; i < x; i += 3;) {
            i->PrintLine();
        }        
~#
              
        each(i : FloatRange->New(0.1, x, 0.2)) {
            i->PrintLine();
        };
    }
}