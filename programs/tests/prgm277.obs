class Repl {
    function : Main(args : String[]) ~ Nil {
        x := 21;
#~
        for(i := 5; i < x; i += 3;) {
            i->PrintLine();
        }        
~#
              
        each(i : CharRange->New('a','f',2)) {
            i->PrintLine();
        };
    }
}