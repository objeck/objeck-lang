class Test {
    function : Main(args : String[]) ~ Nil {
        x := 23.5;
#~
        for(i := 5; i < x; i += 3;) {
            i->PrintLine();
        }        
~#
        r := FloatRange->New(10., x, .5);
        each(i : r) {
            i->PrintLine();
        };
    }
}