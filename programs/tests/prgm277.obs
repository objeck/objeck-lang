class Repl {
    function : Main(args : String[]) ~ Nil {
        x := 21;
#~
        for(i := 5; i < x; i += 3;) {
            i->PrintLine();
        }        
~#
              
        r := FloatRange->New(0.1, 1.5, 0.2);
        each(i : r) {
            i->PrintLine();
        };
    }
}