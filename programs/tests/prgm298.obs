use Data.CSV;

class Test {
	function : Main(args : String[]) ~ Nil {
		System.Time.Date->New(1713061290)->ToString()->PrintLine();
		timestamp := System.Time.Date->New(false)->GetUnixTime();

		timestamp->PrintLine();
		System.Time.Date->New(timestamp)->ToString()->PrintLine();
	}
}