class Foo {
	function : Main(args : String[]) ~ Nil {
		if(args->Size() = 1) {
			Query.Structured.Table->FromFilesystem("foo", args[0])->Query("select name from foo where name like '%.obl'")->ToString()->PrintLine();
		};
	}
}