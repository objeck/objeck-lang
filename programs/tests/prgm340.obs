use System.IO;

class Test {
	function : Main(args : String[]) ~ Nil {
		p := Pipe->New("mynamedpipe", Pipe->Mode->OPEN);
		if(p->IsOpen()) {
			p->WriteString("Hello World!\r\n");
			p->Close();
		};
	}
}