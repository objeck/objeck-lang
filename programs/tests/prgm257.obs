use Collection;
use System.ML;

class Test {
	function : Main(args : String[]) ~ Nil {
		normal := Map->New()<String, BayesElement>;
		normal->Insert("dear", BayesElement->New("dear", 8));
		normal->Insert("friend", BayesElement->New("friend", 5));
		normal->Insert("lunch", BayesElement->New("lunch", 3));
		normal->Insert("money", BayesElement->New("money", 1));
		normal_prob := 8.0 / 12.0;

		spam := Map->New()<String, BayesElement>;
		spam->Insert("dear", BayesElement->New("dear", 2));
		spam->Insert("friend", BayesElement->New("friend", 1));
		spam->Insert("lunch", BayesElement->New("lunch", 0));
		spam->Insert("money", BayesElement->New("money", 4));
		spam_prob := 4.0 / 12.0;

		bt := NaiveBayes->New(normal, normal_prob, spam, spam_prob);
		bt->Query();
	}
}