class Test {
	function : Main(args : String[]) ~ Nil {
		base_dir := "C:\\Users\\objec\\Documents\\Code\\objeck-web-server\\html\\";

		from_str := base_dir->Copy();
		from_str += "gear.jpg";

		to_str := base_dir->Copy();
		to_str += "upload.jpg";

		System.IO.Filesystem.File->Copy(from_str, to_str, true)->PrintLine();
#		System.IO.Filesystem.Directory->Copy("bar", "foo", true)->PrintLine();
	}
}
