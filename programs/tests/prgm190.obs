class Test {
    function : Main(args : String[]) ~ Nil {
        pairs :=  "fname=Randy+Jason&lname=Hollines"->Split('&');
        each(i : pairs) {
            pairs[i] := Web.HTTP.Url->Decode(pairs[i])->ReplaceAll('+', ' ');
        };

        each(i : pairs) {
            pairs[i]->PrintLine();
        };
    }
}