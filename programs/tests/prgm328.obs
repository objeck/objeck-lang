use System.IO.Net, Collection;

class Udp {
	function : Main(args : String[]) ~ Nil {
		server := UDPSocketServer->New(4013);
		server->Bind();
#~		
		if(args->Size() = 1) {
			target := args[0];

			if(target->Equals("client")) {
				DoClient();
			}
			else if(target->Equals("server")) {
				DoServer();
			}
		}
~#		
	}

	function : DoClient() ~ Nil {
		
	}

	function : DoServer() ~ Nil {
		
	}
}