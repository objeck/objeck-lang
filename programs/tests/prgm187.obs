class Test {
    function : Main(args : String[]) ~ Nil {
        map := Collection.Map->New()<String, String>;
        map->Insert("foo", "bar");

        fie := map->Find("foo") + '#' + "foo";
        fie->PrintLine();
    }
}