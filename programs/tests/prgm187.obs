class Test {
    function : Main(args : String[]) ~ Nil {
        map := Collection.Generic.Map->New()<String, String>;
        map->Insert("foo", "bar");

        fie := map->Find("foo") + '#' + "foo";
        fie->PrintLine();
    }
}