class Test {
  function : Main(args : String[]) ~ Nil {
#    "Welcome"->PrintLine();
    Add(9.13)->PrintLine();
  }

  function : native : Add(a : Float) ~ Float {
    a->PrintLine();
    return a * 2.25;
  }
}
