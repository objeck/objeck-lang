class Test {
	function : Main(args : String[]) ~ Nil {
		"Hello"->TypeOf(String)->PrintLine();	
		
		a := "Hello";
		a->TypeOf(String)->PrintLine();
		
		a := "Hello";
		b := a->TypeOf(String)
		b->PrintLine();
	
	}
}