bundle Default {
	class Hello {
		function : Main(args : String[]) ~ Nil {
			Run();		
		}

		function : native : Run() ~ Nil {
			a := 10;
			b := a + 3;
	
			c : Int;
			for(i := 0; i < 5; i += 1;) {
				a := a - b * 5;
				c -= 1;
			};
		}
	}
}
