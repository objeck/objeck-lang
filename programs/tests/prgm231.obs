class Test {
	function : Main(args : String[]) ~ Nil {

		a : Base := "Freak Nasty                                ";
		b := a->As(String)->Capacity()->Abs();
		b->PrintLine();

		
		c := String->New("Hello World!");
#		c->Capacity()->Abs()->PrintLine();
		c->As(String)->Capacity()->Abs()->PrintLine();
	}
}