class Test {
	function : Main(args : String[]) ~ Nil {
#~		
		strings := Collection.Generic.Vector->New()<String>;
		strings->AddBack("One");
		strings->AddBack("Three");
		strings->AddBack("Five");
		strings->AddBack("Seven");
		strings->AddBack("Nine");
		strings->Each(\^(s) => s->PrintLine());
~#		

		map := Collection.Generic.Map->New()<IntHolder, String>;
		map->Insert(1, "One");
		map->Insert(3, "Three");
		map->Insert(5, "Five");
		map->Insert(7, "Seven");
		map->Insert(9, "Nine");
		map->Each(\^(k, v) => "{$k}: {$v}"->PrintLine());
			
	}
}