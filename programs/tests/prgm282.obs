use Collection;

class Test {
    function : Main(args : String[]) ~ Nil {
        v1 := $"c:\documents\files\u0066.txt";
        v2 := "c:\\documents\\files\\u0066.txt";
        v1->Equals(v2)->PrintLine();

        dj_name := "Scratch";
        name := "DJ {$dj_name}";
        name += $" and, 'Double L' must rock (without) the \a\a's";
        name->PrintLine();
        name->SubString(2)->PrintLine();
        name->Size()->PrintLine();
    }
}