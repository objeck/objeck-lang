use Collection;

bundle Abc {
class Test {
	enum Type := -100 {
		A,
		B,
		C
	}

	function : Main(args : String[]) ~ Nil {
#		Type->A->PrintLine();
#		Foo->New();
	}
}

class Foo {
	enum Type := 100 {
		A,
		B,
		C
	}

	New() {
		tt := Abc.Test->Type->New[2];
#		tt := Type->New[2];
		tt->Size()->PrintLine();
	}
}
}