class Test {
    function : Main(args : String[]) ~ Nil {
        t := Tuples.TuplePair->New(1, "One")<IntRef, String>;
        t->GetFirst()->PrintLine();
        t->GetSecond()->PrintLine();
    }
}