class ColorCode {
	#~
	Fii
	@bar dfdk
	@foo back
	~#
	function : Bar(args : String[]) ~ Nil {
		"hi ya"->PrintLine();
	}
	
		#~
		Fii
		@bar <dfdk></a>
		@foo back
		~#
	function : Main(args : String[]) ~ Nil {
		options := Collection.Map->New()<String, String>;
		options->Insert("function-space", "false");
		options->Insert("ident-space", "3");
		options->Insert("trim-trailing", "true");

		x := 4 <   7;
		x->PrintLine();

		y : Int[];
		z := y[2];

		source := System.IO.Filesystem.FileReader->ReadFile(args[0]);
		formatter := System.Utility.CodeFormatter->New(options);
	#	formatter->Format(source, true)->PrintLine();
		formatter->Format(source, true)->PrintLine();
		
	}
}