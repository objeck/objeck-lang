use Web.HTTP;
use Collection.Generic;

class Hello {
	function : Main(args : String[]) ~ Nil {
		headers := Hash->New()<String, String>;

		client := HttpsClient->New();
		client->SetHeaders(headers);

		client->GetAll("")->PrintLine();
	}
}
