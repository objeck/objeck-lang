bundle Default {
	class Parse {
		function : Main(args : String[]) ~ Nil {
			tokens := "Hello,How,Are,You,Today"->Split(",");
			tokens->Size()->PrintLine();
			each(i : tokens) {
				tokens[i]->PrintLine();
			};
		}
	}
}

