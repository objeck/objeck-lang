class Test  {
	function : Main(args : String[]) ~ Nil {
		x := 9;
		a := "a" + (x * 4) + 'g';
		y := String->New[2];
		Bar(a + y[0], "b{$a}" + 2 + 'd');
	}

	function : Bar(a : String, b : String) ~ Nil {
		a->PrintLine();
		b->PrintLine();
	}
}