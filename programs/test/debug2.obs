consts Flags {
	A := 2, B := 4
}

class Test {
	function : Main(args : String[]) ~ Nil {
    	DoIt(13, "foo", 0.0);
	}

	function : DoIt(a : Int, b : String := "Hello", c : Float := 1.3, d : Flags := Flags->B) ~ Nil {
		a->PrintLine();
		b->PrintLine();
		c->PrintLine();
		d->PrintLine();
	}
}
