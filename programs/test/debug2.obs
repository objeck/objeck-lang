use Collection;

class Test {
	function : Main(args : String[]) ~ Nil {
#~		
		bar := rand() % 3;	    
	    select( bar ) {
~#

	    select( rand() % 3 ) {
	        label 0: { "-- 0 --"->PrintLine(); }
	        label 1: { "-- 1 --"->PrintLine(); }
	        label 2: { "-- 2 --"->PrintLine(); }
	    };
	}

	function : rand() ~ Int {
		return Int->Random(0, 32767);
	}
}