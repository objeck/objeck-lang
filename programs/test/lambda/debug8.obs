use Collection.Generic;

lambdas Foo {
	Bar : (IntHolder) ~ IntHolder,
	Fie : (Int) ~ Int
}

class Bar {
	function : Doit() ~ (Float) ~ Int {
		return Foo(Float) ~ Int;
	}

	function : Foo(f : Float) ~ Int {
		return 13;
	}
}

