
class Test {
	x : static : Int;

	function : Main(args : String[]) ~ Nil {
		fu : Function := (a : Int) ~ Int;

		x := 45;

#		f := Foo(Int, Int) ~ Int;
		f := fu => x + a * 13;
		Print(f);
#		Print((a : Int) ~ Int => a * 13 + x);
	}
	
	function : Print(fu ~ Int) ~ Nil {
		f(3)->PrintLine();
	}

	function : Foo(a : Int) ~ Int {
		return a * 13;
	}

	function : Foo(a : Int, b : Int) ~ Int {
		return a + b;
	}
}

