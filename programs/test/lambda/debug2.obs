
class Test {
	function : Main(args : String[]) ~ Nil {
		x := 7;
		y := 13;
		f := (x, y) => x * x + y;
#		f := (x : Int, y : Int) => x * x + y;
#		g := (x) + 3;
#		g->PrintLine();
		Print(f);
	}
	
	function : Foo(a : Int) ~ Int {
		return a * 13;
	}

	function : Foo(a : Int, b : Int) ~ Int {
		return a + b;
	}
}

