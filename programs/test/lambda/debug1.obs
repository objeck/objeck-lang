
class Test {
	function : Main(args : String[]) ~ Nil {
#		f := Foo(Int) ~ Int;
		f := \(Int) ~ Int : (a) => a * a;
		DoIt(f);
	}

	function : Foo(a : Int) ~ Int {
		return a * a;
	}

	function : DoIt(f : (Int) ~ Int) ~ Nil {
		f(12)->PrintLine();
	}
}

