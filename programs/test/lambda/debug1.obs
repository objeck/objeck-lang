lambdas Funcs {
	Apply : (Int) ~ Int
}

class Test {
	function : Main(args : String[]) ~ Nil {
#		f := Square(Int) ~ Int;
		f := \Funcs->Apply : (a) => a * a;
		PrintIt(f);
	}
	
	function : Square(a : Int) ~ Int {
		return a * a;
	}

	function : PrintIt(f : (Int) ~ Int) ~ Nil {
		f(12)->PrintLine();
	}
}

