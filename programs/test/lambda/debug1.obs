
class Test {
	x : static : Int;

	template Funcs { # Will need to read from libs
		foo : (IntHolder, Int, Float) ~ String,
		bar : (IntHolder, Int, Float) ~ Int
	}

	function : Main(args : String[]) ~ Nil {
		x := 45;

		foo : (IntHolder, Int, Float) ~ String;

		f := Foo(Int) ~ Int;
#		f := foo => x + a * 13; # TODO error handling
		Print(f);
#		Print((a : Int) ~ Int => a * 13 + x);
	}
	
	function : Print(f : (Int) ~ Int) ~ Nil {
		f(3)->PrintLine();
	}

	function : Foo(a : Int) ~ Int {
		return a * 13;
	}

	function : Foo(a : Int, b : Int) ~ Int {
		return a + b;
	}
}

