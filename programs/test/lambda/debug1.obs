
class Test {
	function : Main(args : String[]) ~ Nil {
#		f := Foo(Int, Int) ~ Int;
		f := (a : Int, b : Int) ~ Int => a * b;
		
		f(3, 5)->PrintLine();
	}
	
	function : Foo(a : Int, b : Int) ~ Int {
		return a + b;
	}
}

