
class Test {
	x : static : Int;

	templates Funcs { # Will need to read from libs
		foo : (IntHolder, Int, Float),
		bar : (IntHolder, Int, Float)
	}

	function : Main(args : String[]) ~ Nil {
		x := 45;

		foo : (IntHolder, Int, Float) ~ String;

#		f := Foo(Int, Int) ~ Int;
		f := foo => x + a * 13;
		Print(f);
#		Print((a : Int) ~ Int => a * 13 + x);
	}
	
	function : Print(f : (Int) ~ Int) ~ Nil {
		f(3)->PrintLine();
	}

	function : Foo(a : Int) ~ Int {
		return a * 13;
	}

	function : Foo(a : Int, b : Int) ~ Int {
		return a + b;
	}
}

