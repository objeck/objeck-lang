class Foo {
	enum Bar {
		ONE,
		TWO		
	}

	function : Main(args : String[]) ~ Nil {
		handler := H1(Foo->Bar) ~ Nil;
		handler(Foo->Bar->ONE);
	}
    
	function : H1(v : Foo->Bar) ~ Nil {
	}

	function : H2(v : Int) ~ Nil {
	}
}
