use Collection.Generic;

class Test {
	function : Main(args : String[]) ~ Nil {
		v := Vector->New()<String>;
		v->AddBack("Fie");
		v->AddBack("Fum");
		v->AddBack("Foo");
		v->AddBack("SG-1");
		v->AddBack("And");
		v->AddBack("X-File");
		
		v->Sort();

		each(i : v) {
			v->Get(i)->PrintLine();
		};
	}
}