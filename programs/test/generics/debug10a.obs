use Collection.Generic;

class Bar {
	New() {}
}

class Foo {
	function : Main(args : String[]) ~ Nil {
		v := Vector->New()<String>;
		vv := Vector->New(Foo())<String>;
		vv->AddBack("foe");
	}

	function : Foo() ~ Vector<String> {
		return Vector->New()<String>;
	}
}