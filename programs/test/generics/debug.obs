class Test {
	function : Main(args : String[]) ~ Nil {
		a := 3;
		b := a and 7;
#		Fie(IntHolder->New(17));
#		99->PrintLine();
#		x := IntHolder->New(17);
#		Fum(x,3);
#		Fie(IntHolder->New(17));
#		h := FloatHolder->New(3.5);
#		h : FloatHolder := Foo() + 3.5;
#		h->Get()->PrintLine();

#		i : Int := Bar();
# 		i->PrintLine();
#		i : Int := Bar() - h + 'T';
#		i->PrintLine();
	}

	function : Foo() ~ Float {
		return 7.3;
	}

	function : Bar() ~ FloatHolder {
		return FloatHolder->New(13.5);
	}

	function : Fie(h : IntHolder) ~ Nil {
		i : Int := h;
		i->PrintLine();
#		h->Get()->PrintLine();
	}

	function : Fum(i : Int, h : IntHolder) ~ Nil {
		(h+i)->PrintLine();
	}
}