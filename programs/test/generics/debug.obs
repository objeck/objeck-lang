class Test {
	function : Main(args : String[]) ~ Nil {
#		a := 9;
		a := IntHolder->New(9);

		b : Int := a + 3;
#		b : Int := 3 + Bar();
		b->PrintLine();

		beer := Stout() * Ale(11);
		beer->PrintLine();
	}

	function : Fee() ~ Int {
		return 13;
	}

	function : Fie() ~ IntHolder {
		return IntHolder->New(7);
	}

	function : Foo(i : Int) ~ Nil {
		i->PrintLine();
	}

	function : Fum(h : IntHolder) ~ Nil {
		h->Get()->PrintLine();
	}

	function : Stout() ~ Int {
		return IntHolder->New(11);
	}

	function : Ale(a : Int) ~ IntHolder {
		return 5 + a;
	}
}