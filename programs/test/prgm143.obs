use System.Diagnostics;

class Test {
	function : Main(args : String[]) ~ Nil {
		analyzer := Analyzer->ParseFile("../../../../programs/test/prgm143.obs","collect.obl");
		if(analyzer->WasParsed()) {
			symbols := analyzer->GetSymbols();
			each(i : symbols) {
				s := symbols[i]->GetType();
				s->PrintLine();
			};
		};
		analyzer->Release();
	}
}