use System.IO.File;

class Test {
	function : Main(args : System.String[]) ~ Nil {
#		f := "./readme.htm";
		f := ".";
		File->GetFullName(f)->PrintLine();
		Directory->Exists(".")->PrintLine();
		File->Exists(f)->PrintLine();
	}
}
