class CRC32 {
	function : Main(args : String[]) ~ Nil {
		a := 101;
		select(a) {
			label 101: {
				"-- 101 --"->PrintLine();
			}

			label 102: {
				"-- 102 --"->PrintLine();
			}		
		};

		buffer := "foo";
		buffer += Bar()->ToString();
		buffer->PrintLine();
	}

	function : Bar() ~ Int {
		return 13;
	}
}