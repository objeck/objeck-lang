use System.IO.File;

class Test {
	function : Main(args : String[]) ~ Nil {
		dir := Runtime->GetProperty("install_dir");
		dir += "/lib/sdl/fonts/hella.ttf";
		dir->PrintLine();
		File->Exists(dir)->PrintLine();
	}
}
