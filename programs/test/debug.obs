class Test {
	function : Main(args : String[]) ~ Nil {
		bytes := "Ain't nuttin but a G thing"->ToByteArray();
		compressed := bytes->Compress();
		compressed->Size()->PrintLine();
		bytes->CRC32()->PrintLine();
	}
}
