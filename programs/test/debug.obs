use Game.SDL2;

class Test {
	function : Main(args : String[]) ~ Nil {
		timeout := Timer->GetTicks() + 4000;
		while(<>Timer->Passed(Timer->GetTicks(), timeout)) {
		};

		"All done"->PrintLine();
	}
}
