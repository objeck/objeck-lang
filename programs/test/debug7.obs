class Foo {
	function : Main(args : String[]) ~ Nil {
		a := Vector->New()<String>;
		a->AddBack("FloatHolder->New(13.5)");
		a->AddBack("FloatHolder->New(33.3)");
		
		a->Size()->PrintLine();
		
		b := a->Apply(Bar(String) ~ String)<String>;
		a->Get(1)->PrintLine();
		b->Get(1)->PrintLine();
	}
	
	function : Bar(h : String) ~ String {
		h += "#";
		return h;
	}
}
