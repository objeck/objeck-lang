bundle Default {
	class Hello {
		function : Main(args : System.String[]) ~ Nil {
			"hello world"->PrintLine();
			"Hello WorLd"->ToUpper()->PrintLine();
			"Hello WorLd"->ToLower()->PrintLine();
			
			s := "objeck";
			s->Append('-');
			s->Append('l');
			s->Append('a');
			s->Append('n');
			s->Append('g');
			s->PrintLine();
			s->SubString(5)->PrintLine();

			a := 3 < 4;
			a->PrintLine();

			(3 - 4)->PrintLine();

			"Hello WorLd"->Reverse()->PrintLine();
			"a"->Reverse()->PrintLine();
			""->Reverse()->PrintLine();
			"ab"->Reverse()->PrintLine();

		}
	}
}
