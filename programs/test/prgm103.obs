use Data.JSON;

class Hello {
	function : Main(args : String[]) ~ Nil {
		foo := JSONElement->Decode("Finding Twitter webhooks a challenge to set up? With Autohook, it\\u2019s so easy you can actually build a complete autor\\u2026 https:\\/\\/t.co\\/srRymga55Y");
		foo->PrintLine();

		bar := JSONElement->Encode(foo);
		bar->PrintLine();		
	}
}
