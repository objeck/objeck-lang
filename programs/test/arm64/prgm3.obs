class Test {
	function : Main(args : String[]) ~ Nil {
		Foo(99,13)->PrintLine();
	}

	function : native : Foo(a : Int, b : Int) ~ Int {
		c : Int;
		d : Int;
	
		d += 1;
		do {
			a += 1;
			c += a + 2;
		}
		while(a < 101);

		return c + d;
	}
}
