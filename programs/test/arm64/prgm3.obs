class Test {
	function : Main(args : String[]) ~ Nil {
		Foo(99,13)->PrintLine();
	}

	function : native : Foo(a : Int, b : Int) ~ Int {
		c : Int;

		while(a < 101) {
			c := 13 + a;
			a += 1;
		};

		return c;
	}
}
