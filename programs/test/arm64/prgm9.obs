class Test {
	function : Main(args : String[]) ~ Nil {
		Foo(10);
	}

# function : native : Foo(a : Int) ~ Nil {
	function : Foo(a : Int) ~ Nil {
		select(a) {
			label 5: {
				"-5-"->PrintLine();
			}

			label 13: {
				"-13- "->PrintLine();
			}

			label 7: {
				"-7-"->PrintLine();
			}

			other: {
				"other"->PrintLine();
			}
		};
	}
}
