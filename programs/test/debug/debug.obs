class Test {
	function : Main(args : String[]) ~ Nil {
		map := Collection.Generic.Hash->New()<IntHolder, IntHolder>;
		map->Insert(1, 101);
		map->Insert(1, 201);
		map->Size()->PrintLine();

		map->Remove(1);
		map->Find(1)->PrintLine();
	}
}
