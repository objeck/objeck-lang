use Data.JSON;

class Foo {
	function : Main(args : String[]) ~ Nil {
		p := JSONParser->New("{\"jsonrpc\":\"2.0\",\"method\":\"initialized\",\"params\":{}}");
		if(p->Parse()) {
			pp := p->GetRoot();
			ppp := pp->Get("params");
			ppp->ToString()->PrintLine();
		};
    }
}
