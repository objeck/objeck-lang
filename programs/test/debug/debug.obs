use Data.JSON;
use Collection.Generic;

class Foo {
	function : Main(args : String[]) ~ Nil {
		@keywords := Vector->New()<String>;
		@keywords->AddBack("method");
		@keywords->AddBack("function");
		@keywords->AddBack("Int");
		@keywords->AddBack("Float");

		response := JSONElement->New(JSONType->ARRAY);
		each(i : @keywords) {
#			item := response := JSONElement->New(JSONType->OBJECT);
#			item->Insert("label", "TypeScript");
#			item->Insert("kind", "CompletionItemKind.Text");
#			item->Insert("data", "1");
			response->Add(1);
		};
		response->ToString()->PrintLine();
    }
}
