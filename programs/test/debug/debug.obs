class Test {
	function : Main(args : String[]) ~ Nil {
#		x := String->New();
#		a := x->As(Int);

		a := Bar()->As(Int[]);
		a->PrintLine();

	}

	function : Bar() ~ Byte[] {
		return Nil;
	}

	enum Foo {
		BAR
	}
}