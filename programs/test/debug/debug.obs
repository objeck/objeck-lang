use Collection.Generic;

bundle Donut {
	class Foo {
		New() {}
	}
}


	class Foo {
		New() {}
	}

	class : public : Test {
		function : Main(args : String[]) ~ Nil {
			m := Map->New()<String, String>;
			m->Insert("1", "One");

			n := TreeNode->New("1", "One", 0);
			d := Donut.Foo->New();
		}

		function : native : public : Bar() ~ Int {
			return 13;
		}
	}
