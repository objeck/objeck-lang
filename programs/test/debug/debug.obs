use Collection;

bundle Donut {
	class : private : Foo {
		New() {}
	}
}

bundle Donut {
	class : private : Foo {
		New() {}
	}
	
	class : public : Test {
		function : Main(args : String[]) ~ Nil {
			m := Map->New();
			m->Insert("1", "One");

			n := TreeNode->New("1", "One", 0);
		}

		function : Bar() ~ Int {
			return 13;
		}
	}
}