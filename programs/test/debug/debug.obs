use Data.JSON;
use Collection.Generic;

class Foo {
	function : Main(args : String[]) ~ Nil {
		length := 13;
		text := "abc";
		chars := "Content-Length: {$length}\r\n\r\n{$text}"->ToCharArray();
chars->Size()->PrintLine();
    }
}
