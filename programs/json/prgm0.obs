use Data.JSON;
use Collection.Generic;

class Test {
	function : Main(args : String[]) ~ Nil {
		builder := JsonBuilder->New();
		builder->PushArray();
		each(i : 3) {
			builder->PushObject()->Insert("name", i + 1)->Pop();
		};
		builder->Pop()->ToString()->PrintLine();
	}
}