# EXPECT_COMPILE_ERROR: Invalid expression: unexpected token
#~
The unsigned suffix is meaningless on a floating-point literal and must not
be accepted silently.
~#

class BadUnsignedSuffixOnFloat {
	function : Main(args : String[]) ~ Nil {
		value := 1.5u;
		value->PrintLine();
	}
}
