# Linked list class for Objeck language
# R.Hollines

use Structure;

bundle Default {
	class Test {
		function : Main(args : System.String[]), Nil {
			Run();
		}
	
		function : native : Run(), Nil {
#		function : Run(), Nil {
			list : IntLinkedList := IntLinkedList->New();

			list->AddBack(12);
			list->AddBack(13);
			list->AddBack(18);
			list->AddFront(1);

			while(list->IsEnd() <> true) {
				list->GetValue()->PrintLine();
				list->Next();
			};

			list->Foward();
			while(list->IsStart() <> true) {
				list->GetValue()->PrintLine();
				list->Previous();
			};
			
			"size: "->Print();
			list->GetSize()->PrintLine();
		}		
	}
}

