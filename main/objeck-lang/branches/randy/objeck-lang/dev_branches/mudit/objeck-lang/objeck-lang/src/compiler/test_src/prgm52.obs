bundle Default {
	class Hello {
		New() {
		}

		function : Main(args : System.String[]), Nil {
			Run(args);
		}

		function : native : Run(args : String[]), Nil {
			t := Time->New();
			t->GetHours()->PrintLine();
			t->GetMinutes()->PrintLine();
		}
	}
}
